i have used the QR generate package name simple-qrcode for QR generation
in controller i have genrated the qr code.


<!-- installation -->

git cline simple-qrcode for QR generation
cd project
composer install
php artisan migrate:fresh --seed
sudo apt install -y php-imagick
sudo systemctl restart php8.1-fpm
php artisan serve

for the QR scanning kindly use Ngrok on the local system.
ngrok http 8000
